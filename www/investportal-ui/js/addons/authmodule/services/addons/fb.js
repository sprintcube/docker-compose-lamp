class FacebookDataAccess{
    constructor(service){
        this.service = service;
    }

    proccess(query){
        var isSIN = this.service === 'SignIn',
            isSUP = this.service === 'SignUp';

        
    }
    activate(dataQuery){
        var isSIN = this.service === 'SignIn',
            isSUP = this.service === 'SignUp';
    }
}